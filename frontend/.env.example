# ============================================================
# Copia questo file in .env e compila i valori reali
# Il file .env NON deve mai essere committato in git
# ============================================================

# URL base del backend (senza slash finale)
VITE_API_BASE=http://localhost:3005/api/v2

# Password di accesso all'area Amazon (se usata)
VITE_AMAZON_PASSWORD=cambia_questa_password
